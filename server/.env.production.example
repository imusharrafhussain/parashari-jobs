# Production Environment Variables Configuration

## Core Server Configuration
PORT=5000
NODE_ENV=production
LOG_LEVEL=info

## MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

## Email Service Configuration

### Option 1: Gmail (Development/Testing)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
HR_EMAIL=hr@company.com

### Option 2: SendGrid (Recommended for Production)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxx
# SENDGRID_FROM_EMAIL=noreply@astrobharatai.com
# SENDGRID_FROM_NAME=AstroBharatAI Careers

## Frontend Configuration
FRONTEND_URL=https://astrobharat-jobs-portal.vercel.app

## Security Configuration

### JWT Secret (REQUIRED - Generate with: openssl rand -base64 64)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-in-production

### Admin Authentication (REQUIRED)
ADMIN_USERNAME=admin

# Generate password hash with:
# node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('YOUR_PASSWORD', 10, (err, hash) => console.log(hash))"
ADMIN_PASSWORD_HASH=$2a$10$EXAMPLE_HASH_REPLACE_WITH_ACTUAL_BCRYPT_HASH

## Optional Rate Limiting Configuration (Defaults are provided in code)
# RATE_LIMIT_WINDOW_MS=600000
# RATE_LIMIT_MAX_REQUESTS=5
